# Backend

Express API for ResumeForge.

## Run
```bash
npm install
npm start
```

## Endpoints
- `POST /api/analyze` (multipart form-data: `resume`, `jobDescription`)
- `POST /api/feedback`
- `GET /api/history`
